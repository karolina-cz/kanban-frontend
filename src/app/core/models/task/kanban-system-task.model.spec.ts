import { KanbanSystemTask } from './kanban-system-task.model';

describe('KanbanSystemTask', () => {
  it('should create an instance', () => {
    expect(new KanbanSystemTask()).toBeTruthy();
  });
});
